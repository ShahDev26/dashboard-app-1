# Required: the shared password gating the whole dashboard.
# Set this in the Vercel project Settings -> Environment Variables.
DASHBOARD_PASSWORD=

# Upstash Redis variables (auto-set by the Upstash Redis integration
# from the Vercel Marketplace — do not set manually unless dev-only):
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
